{{!< desktop}}

<div class="container">
    <div class="registration-page">
        <form class="registration-page__top">
            {{> "page-title/pageTitle" title="Регистрация"}}
            {{> "input/input" name="email" fieldName="Почта" type="text"}}
            {{> "input/input" name="login" fieldName="Логин" type="text"}}
            {{> "input/input" name="first_name" fieldName="Имя" type="text"}}
            {{> "input/input" name="second_name" fieldName="Фамилия" type="text"}}
            {{> "input/input" name="phone" fieldName="Телефон" type="tel"}}
            {{> "input/input" name="password" fieldName="Пароль" type="password"}}
            {{> "input/input" name="password" fieldName="Подтердить пароль" type="password"}}
        </form>
        <div class="registration-page__bottom">
            {{> "button/button" style="primary fill" text="Зарегестрироваться" type="submit"}}
            {{> "link/link" text="Войти" href="authorization.hbs"}}
        </div>
    </div>
</div>
